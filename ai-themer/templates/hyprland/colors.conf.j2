##############
### COLORS ###
##############

# Generated by AI Themer from {{ palette.source_image or "unknown" }}
# Extraction method: {{ palette.extraction_method or "unknown" }}
# Quality score: {{ palette.quality_score or "unknown" }}

# Border colors - used in appearance.conf
$active_border = rgba({{ colors.primary.hex[1:] }}ee) rgba({{ colors.accent.hex[1:] if colors.accent else colors.primary.hex[1:] }}ee) 45deg
$inactive_border = rgba({{ colors.background.hex[1:] }}aa)

# Shadow colors - used in appearance.conf
$shadow_color = rgba({{ colors.background.hex[1:] }}ee)

# Additional color variables for consistency
$bg_color = rgba({{ colors.background.hex[1:] }}ff)
$fg_color = rgba({{ colors.text.hex[1:] }}ff)
$accent_color = rgba({{ colors.accent.hex[1:] if colors.accent else colors.primary.hex[1:] }}ff)
$urgent_color = rgba({{ colors.accent.hex[1:] if colors.accent else colors.primary.hex[1:] }}ff)