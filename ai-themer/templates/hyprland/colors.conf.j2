# Hyprland Color Configuration
# Generated by AI Themer from {{ palette.source_image or "unknown" }}
# Extraction method: {{ palette.extraction_method or "unknown" }}
# Quality score: {{ palette.quality_score or "unknown" }}

# General window border colors
general {
    col.active_border = {{ colors.primary.hex }}
    col.inactive_border = {{ colors.background.hex }}
    col.nogroup_border = {{ colors.accent.hex if colors.accent else colors.primary.hex }}
    col.nogroup_border_active = {{ colors.primary.hex }}
}

# Window decoration colors
decoration {
    # Shadow colors
    shadow {
        color = {{ colors.background.hex }}
        color_inactive = {{ colors.background.hex }}
    }
}

# Group window border colors
group {
    col.border_active = {{ colors.primary.hex }}
    col.border_inactive = {{ colors.background.hex }}
    col.border_locked_active = {{ colors.accent.hex if colors.accent else colors.primary.hex }}
    col.border_locked_inactive = {{ colors.secondary.hex if colors.secondary else colors.background.hex }}
}

# Groupbar colors
group {
    groupbar {
        text_color = {{ colors.text.hex }}
        text_color_inactive = {{ colors.background.hex }}
        text_color_locked_active = {{ colors.text.hex }}
        text_color_locked_inactive = {{ colors.background.hex }}
        col.active = {{ colors.primary.hex }}
        col.inactive = {{ colors.background.hex }}
        col.locked_active = {{ colors.accent.hex if colors.accent else colors.primary.hex }}
        col.locked_inactive = {{ colors.secondary.hex if colors.secondary else colors.background.hex }}
    }
}

# Misc colors
misc {
    col.splash = {{ colors.primary.hex }}
    background_color = {{ colors.background.hex }}
} 